# Python Requirements for SDLC Claude Code
#
# This file documents Python dependencies used in the Claude Code Docker container.
# The container uses Python 3 with pip and venv for any Python-based tooling.
#
# Note: Most dependencies are installed at the Docker image level via apt.
# This file is provided for reference and optional local development use.

# Core dependencies (installed via apt in Dockerfile)
# - python3
# - python3-pip
# - python3-venv

# Optional: If you need to extend the container with Python packages, add them below:
# Example:
# requests>=2.31.0
# pyyaml>=6.0
